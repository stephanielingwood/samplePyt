
# Language setting
language: python

# Version number
python:
  - 2.6
  - 2.7
  
matrix:
  include:
    - python: 3.2
  exclude:
    - python: 2.7
  allow_failures:
    - compiler: "gcc"
  

build:
  ci:
    - python --version
    
    


